import { BlockStyleDirective } from './block-style.directive';

describe('BlockStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockStyleDirective();
    expect(directive).toBeTruthy();
  });
});
